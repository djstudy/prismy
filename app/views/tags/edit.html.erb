<%= render partial: "./admin_nav" %>
<h1><%= @tag.name %> 태그 수정</h1>
<%= form_for @tag do |f| %>

  <%= render partial: "form", locals: {f: f} %>
  <%= f.submit "수정", class: "btn btn-success btn-sm"  %>
  <%= link_to "뒤로", tags_path, class: "btn btn-info btn-sm" %>

<% end %>